/**
 * Infrastructure to run suites of tests written in YAML against a running Elasticsearch cluster
 * using "low level" REST client.
 *
 * The YAML tests are run by all official clients and serve as tests for both Elasticsearch and the clients.
 */
package org.elasticsearch.test.rest.yaml;